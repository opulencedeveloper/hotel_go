export enum OrderType {
  RESTAURANT = "restaurant",
  HOTEL_GUEST = "hotel_guest",
  WALK_IN = "walk_in",
}


export enum OrderStatus {
  PENDING = "pending",
  READY = "ready",
  PAID = "paid",
  CANCELLED = "cancelled",
}